import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.geniebikez.app',
  appName: 'GenieBikez',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
